#  Exact digonalization (ED) to solve few sites Hubbard model
**Notice**:
- C++ EIGEN library is employed to construct a sparse matrix format.
- A sparse GPU solver is now supported by MAGMA library (CUDA only).
